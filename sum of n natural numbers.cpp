#include <stdio.h>

int main() {
    int N;
        int sum = 0;

            // Input
                scanf("%d", &N);

                    // Calculate sum of first N natural numbers
                        sum = N * (N + 1) / 2;

                            // Output
                                printf("%d
", sum);

                                    return 0;
                                    }
