#include <stdio.h>

int main() {
    int R, C, E, totalCells;

        // Input
            scanf("%d %d %d", &R, &C, &E);

                // Calculate total cells
                    totalCells = (R + E) * C;

                        // Output
                            printf("%d
", totalCells);

                                return 0;
                                }
