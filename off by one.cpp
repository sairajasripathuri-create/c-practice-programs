#include <stdio.h>

int main() {
    int A, B, sum;

        // Input
            scanf("%d %d", &A, &B);

                // Calculate the actual sum
                    sum = A + B;

                        // The calculator appends an extra '1' at the end
                            printf("%d1
", sum);

                                return 0;
                                }
