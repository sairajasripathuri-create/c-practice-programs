
int main() {
    int C;
        float F;

            // Input
                scanf("%d", &C);

                    // Conversion formula
                        F = (C * 9.0 / 5.0) + 32;

                            // Output with two decimal places
                                printf("%.2f
", F);

                                    return 0;
                                    }
