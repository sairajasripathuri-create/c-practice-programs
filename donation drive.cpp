#include <stdio.h>

int main() {
    int N, X, remaining;

        // Input
            scanf("%d %d", &N, &X);

                // Calculate remaining donations
                    remaining = N - X;

                        // Output
                            printf("%d
", remaining);

                                return 0;
                                }
